CREATE DATABASE 15638_faizu;

CREATE TABLE users (
    id CHAR(6) PRIMARY KEY,
    username VARCHAR(25) NOT NULL,
    password VARCHAR(20) NOT NULL,
    last_login DATETIME,
    active TINYINT(1) NOT NULL DEFAULT 1
);

INSERT INTO users (id, username, password, last_login, active)
VALUES ('U00001', 'admin', 'admin123', NOW(), 1);

INSERT INTO users (id, username, password, last_login, active)
VALUES ('U00002', 'admin', 'pemberani', NOW(), 1);